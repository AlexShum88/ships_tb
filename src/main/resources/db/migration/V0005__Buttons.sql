create table buttons (
                        command text,
                        en text,
                        ru text,
                        primary key (command(200))
                       )